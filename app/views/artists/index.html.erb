<h1><%= @artists.each do |artist| %></h1>
    <ul>
        <li>
            <%= link_to artist.name, artist_path(artist) %> <%= pluralize(artist.song_count, 'song')%> 
         </li>
   </ul>
<% end%>
